# LLM Router Agent Contract

- Follow the canonical global agent and Git contracts resolved by the Workbench registry.
- This repository owns bounded routing, retries, and usage/cost arithmetic only. It is not a
  policy gateway, secret broker, provider-attestation service, or portfolio authority.
- Treat `main` as protected; use a registered `codex/*` task worktree and explicit-path staging.
- Repository instructions may narrow but never weaken global secret, money, evidence,
  destructive-action, and owner-gate boundaries.
- Never read, print, copy, or commit credentials; provider calls require fresh explicit authority.
- Model output is advisory. Deterministic tests own routing and budget arithmetic claims.
- Merge, release, deployment, provider use, enforcement, and destructive Git remain separate
  owner gates. Portfolio and local roadmap documents grant no authority.
- Before completion run the repository test/quality gates and the portfolio contract test.
